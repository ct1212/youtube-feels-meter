# YouTube Feels Meter - Environment Variables

# YouTube API (Required)
# Get your API key: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your-youtube-api-key-here

# Server Configuration
PORT=3001
NODE_ENV=development

# Cache Configuration
# Redis URL (optional - falls back to in-memory if not provided)
REDIS_URL=redis://localhost:6379

# Cache TTL in seconds (default: 30 days)
CACHE_TTL=2592000

# Rate Limiting
# Window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Max requests per window (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# ========================================
# REMOVED (No longer needed!)
# ========================================
# Spotify API keys are no longer required
# We now use MusicBrainz (100% free) + Genre-Based Analysis
#
# No LLM API needed - completely free!
#
# OLD (don't use):
# SPOTIFY_CLIENT_ID=...
# SPOTIFY_CLIENT_SECRET=...
# ANTHROPIC_API_KEY=...
